apiVersion: v1
kind: Service
metadata:
  name: {{ include "helixon-control-plane.fullname" . }}
  labels:
    {{- include "helixon-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helixon-control-plane.selectorLabels" . | nindent 4 }}