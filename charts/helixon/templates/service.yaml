apiVersion: v1
kind: Service
metadata:
  name: {{ include "helixon.fullname" . }}
  labels:
    {{- include "helixon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helixon.selectorLabels" . | nindent 4 }}