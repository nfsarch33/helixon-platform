# Trivy ignore list (false positives, internal-only, accepted risk)
# Format: <vuln-id>|<expiry-date>|<reason>

# Example (uncomment and adapt as needed):
# CVE-2023-12345|2026-12-31|transitive dep, no upgrade path; monitored via govulncheck

# v17702-4: evidence/ contains 1Password exports with masked secrets (`********************`)
# Trivy AWS-access-key-id heuristic false-positives on the literal mask string.
# These are intentional redactions from 1Password export (`additional_information` field),
# NOT real credentials. Exclude entire `evidence/` secret scanning surface.
# Suppression: secret-scan via .trivyignore + trivy.yaml exclusion (added below).
# Owner: cursor-parent; reviewed 2026-07-10.
AVD-AWS-0176|2027-07-10|false-positive on 1Password masked-secret placeholder in evidence/1p-items.json (Trivy AWS key heuristic matches `********************`)