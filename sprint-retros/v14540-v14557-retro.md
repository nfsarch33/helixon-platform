# runx-public-repo-gate: allow-file fleet_host_alias,network_topology,personal_path_id
# v14540-v14557 — Helixon Service Registry + Fleet Operations (Retro)

## Sprint pairs (each pair is MVP → Review)

| #   | Sprint | One-line summary                                                                                       |
|-----|--------|--------------------------------------------------------------------------------------------------------|
| 1   | v14540 | Service registry schema + `registra` Go CLI + ingestion of 6 fragmented files                          |
| 1   | v14541 | `helix-dev-tools services` CLI + `svcregistryd` + `inventory/services/ui/register.html`                |
| 2   | v14542 | win4/wsl4 LAN + Tailscale troubleshooting + k3s agent join (CF-v14529-02)                              |
| 2   | v14543 | OCI web-UI SOP + agent-browser playbook (CF-OP-01)                                                     |
| 3   | v14544 | GitLab CE Helm install on k3s (CF-v14536-01) + runner registration                                    |
| 3   | v14545 | ArgoCD install on k3s with pre-created redis secret (CF-v14536-02) + app-of-apps sync                   |
| 4   | v14546 | 1Password `op` CLI secrets injection — rewrote `bashrc.local` for any remaining plaintext              |
| 4   | v14547 | `engramd` + `sprintboard-api` + `llm-router` systemd user units + AlertManager                         |
| 5   | v14548 | Systemd timers (cleanup-wsl 30m, fleet-doctor 6h, engram-prune 24h)                                    |
| 5   | v14549 | Workspace doctor full PASS + cursor-tools doctor all exit 0 + Evospine cycle (CF-v14532-02/35-01)      |
| 6   | v14550 | 6 new Cursor rules (10+ → 15+) + hooks.json path sync + skills sync to v14522 KB pattern               |
| 6   | v14551 | Workspace hygiene: stale-branch prune + fork-upstream sync (cursor-global-kb & helixon-platform)       |
| 7   | v14552 | runx + cursor-tools fleet agents + agentrace/evospine integration on wsl1                              |
| 7   | v14553 | `fleet-agent.service` + per-node register via `service-registry-register.service`                      |
| 8   | v14554 | `k3s-agent-join.sh` for wsl2/wsl3 + GitLab sentrux CI pipeline (5 stages)                              |
| 8   | v14555 | wsl1 ↔ win2/win4 full mesh verified + 7-panel Grafana dashboard + Prometheus docker-compose            |
| 9   | v14556 | This retro + ADR-0095 (service registry) + ADR-0096 (win4 recovery) + release tag prep                 |
| 9   | v14557 | FINAL Sentrux audit + release tag `sentrux-2026-08-26` + 100% closure gate                             |

## KPIs
- **Commits**: 22 commits across `cursor-global-kb` (main) and
  `helixon-platform` (main + feature branches).
- **New Go binaries**: `registra`, `svcregistry-bridge`, `helixon` (built
  from `cmd/helixon`), `secrets-bootstrap` (v1.1.0).
- **New systemd user units**: `engramd`, `sprintboard-api`, `llm-router`,
  `svcregistryd`, `alertmanager`, `fleet-agent`, `service-registry-register`,
  `cleanup-orphaned-wsl-procs`, `fleet-doctor`, `engram-prune`.
- **New Cursor rules**: 6 alwaysApply/globbed fleet canon rules
  (`01-svcregistry-canon`, `01-systemd-user-convention`,
  `01-1password-uuid-required`, `01-fleet-doctor-cadence`,
  `01-sprint-evidence-required`, `01-tailscale-wsl-mirror`).
- **New ADRs**: ADR-0095 (service registry), ADR-0096 (win4 recovery).
- **New runx skills**: `helixon-fleet-report` (with X.yaml harness cases).
- **New Grafana dashboards**: 1 (`helixon-fleet-mesh-v14555-dashboard.json`,
  7 panels).
- **New CI pipeline**: `ci/sentrux.gitlab-ci.yml` (5 stages).

## Wins
1. **Service registry canon shipped end-to-end**: static SOT, runtime
   daemon, bridge, CLI, browser UI, and a self-registering
   `service-registry-register.service`. No more "which port is X?"
   guesswork.
2. **All 5 fleet services running as systemd user units with
   1Password-injected secrets** — no plaintext in `EnvironmentFile`,
   no display-name lookups.
3. **Mesh verified live** for 3 of 4 nodes (win3 offline). 97ms RTT to
   win2, 39ms RTT to win4 — both well within SLO.
4. **Tailscale + WSL mirror constraints** are now a Cursor rule, an ADR,
   a Grafana dashboard panel, and a SOP — single source of truth.

## Carry-forward items
- **CF-v14555-01**: bring up the Prometheus docker-compose stack
  (`observability/docker-compose.yml up -d`) and verify the Grafana
  panels show data. Blocked on a `docker compose` runner install.
- **CF-v14555-02**: install the k3s agents on wsl2/wsl3 by running
  `scripts/k3s-agent-join.sh` from each node. Requires operator access
  to win2/wsl2 and win3/wsl3.
- **CF-v14555-03**: when the observability stack is up, enable
  AlertManager → Slack routing by injecting
  `SENTRUX_SLACK_WEBHOOK` from 1Password.
- **CF-v14556-01**: bump runx to a version that supports
  `runner_manifest.cases` so the harness X.yaml validates without
  signing-env-var scaffolding.

## Sentrux audit readiness (v14557)
- All 16 prior sprints have `evidence/v<NUMBER>-<slug>/README.md` files.
- All 16 prior sprint commits are pushed to `origin/main`.
- Both repos have no plaintext secrets (verified by `gitleaks`).
- Both repos are clean (`git status --short` shows nothing).
- The release tag `sentrux-2026-08-26` will be cut after the final
  sentrux audit pass in v14557.
